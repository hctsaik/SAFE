# The Safety Net — YOLO -> SAM -> DINO 依賴
# 環境：Python 3.11~3.14；CPU 即可（腳本自動偵測 CUDA 並適配）。
# torch/torchvision 請依平台安裝（CPU: pip install torch torchvision；
#                                  CUDA: 見 https://pytorch.org 選對應 cu 版本）
torch>=2.2
torchvision>=0.17
ultralytics>=8.3        # 提供 YOLO11 + SAM/SAM2/MobileSAM/FastSAM
opencv-python>=4.8
numpy>=1.26
scikit-learn>=1.3
pyyaml>=6.0
streamlit>=1.40     # Safety Net Inspector 視覺化 GUI
plotly>=5.20        # GUI 互動圖表（直方圖 / 溫度計 / PCA 投影）
# DINOv2 透過 torch.hub 自 facebookresearch/dinov2 載入（首次執行自動下載權重）
